# Публичный npm для всех пакетов кроме @mts-ds
registry=https://registry.npmjs.org/

# @mts-ds берётся локально через file: в package.json — строка ниже не нужна
# registry=https://nexus.mgts.ru/repository/npm-all/