Control-list row (Granat Cell) — leading icon, title + description, trailing slot.

```jsx
<ListItem icon="DocumentSize24StyleOutline" title="Договор №2024-118"
  description="ООО «Ростелеком»" trailing={<Badge tone="positive">Согласован</Badge>} onClick={open} />
```

Sizes s/m/l; `selected` tints brand-subtle.
