Input token chip for multi-value fields (recipients, tags). Outlined, removable.

```jsx
<FormChip icon="MailSize24StyleOutline" onRemove={()=>remove(id)}>name@mgts.ru</FormChip>
```

`invalid` turns the border negative.
