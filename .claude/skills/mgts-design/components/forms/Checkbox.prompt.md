Checkbox with МГТС-blue checked fill and optional label.

```jsx
<Checkbox checked={agree} onChange={setAgree} label="Согласен с условиями" />
```

`onChange` receives the next boolean. Supports `disabled`.
