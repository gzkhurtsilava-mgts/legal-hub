Full-width sticky strip for page-level info/promo messages.

```jsx
<StickyBanner tone="brand" message="Плановые работы 28 июня с 02:00 до 04:00" actionLabel="Подробнее" onAction={open} onClose={hide} />
```

Tones: brand, info, warning, neutral.
