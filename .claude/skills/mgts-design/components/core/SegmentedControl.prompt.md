Exclusive segment switch (2–5 options). Grey track, active segment is a white shadowed pill.

```jsx
<SegmentedControl value={view} onChange={setView}
  segments={[{value:'list',label:'Список'},{value:'grid',label:'Плитки'}]} />
```

Sizes s (36) / m (44); `fullWidth` to stretch.
