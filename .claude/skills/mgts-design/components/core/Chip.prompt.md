Selectable pill chip for filters and single/multi choice. Selected fills МГТС blue.

```jsx
<Chip selected onClick={toggle} icon="CheckSize24StyleOutline">Согласованные</Chip>
<Chip onClick={pick}>Черновики</Chip>
```

Sizes s (32) / m (40). Optional `icon`, `onRemove`.
